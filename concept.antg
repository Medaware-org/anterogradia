@library "org.medaware.anterogradia.libs.Strings" as str

`str := "Hello, World!"
`i := |&`str|

while (!(!(&`i > 0))) {
    _debug(str = str.substr(str = &`str, start = &`i))
    decrement(id = `i)
}