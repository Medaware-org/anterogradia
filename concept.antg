
fun variableName <x, y> {
    sequence { "px" &`x " : " &`y }
}

fun getAt <x, y> {
    & eval variableName(x = &`x, y = &`y)
}

fun setAt <x, y, value> {
    eval variableName(x = &`x, y = &`y) := &`value
}
