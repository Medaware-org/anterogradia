@library "org.medaware.anterogradia.libs.HTML"

HTML:html {
    HTML:head {},
    HTML:body {
        HTML:div (
            attributes = HTML:classList { "title-heading" },
            body = sequence {
                HTML:h1 (
                    attributes = nothing(),
                    body = "Hello, World!"
                )
            }
        ),
        HTML:h1 (
            attributes = nothing(),
            body = "Medaware.org"
        )
    }
}